// src/services/stt.service.js
// Simple STT stub. Replace implementation with OpenAI/Google STT when ready.
module.exports.transcribeFromUrl = async function (audioUrl, opts = {}) {
  // Placeholder: return null or "transcript text"
  // In production call OpenAI Whisper / Google STT and return text.
    return null;
};